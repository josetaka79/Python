# Create a list of guests
Guest = ["Fru", "Khan"]

# Print the initial guest list
print(Guest)

# Remove the guest at index 0 ("Fru")
del Guest[0]

# Print the guest list after removing the first guest
print(Guest)

# Remove the next guest at index 0 ("Khan")
del Guest[0]

# Print the guest list after removing the second guest
print(Guest)  # The list is now empty